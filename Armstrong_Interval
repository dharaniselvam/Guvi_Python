N = int(input())
Q = int(input())
temp = 0
for i in range(N+1,Q):
    temp = i
    sum = 0
    while temp>0:
        rem = temp%10
        sum = sum+(rem**3)
        temp = temp//10
    if sum==i:
        print(i)
    
